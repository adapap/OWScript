# Generated from Test.g4 by ANTLR 4.7.1
from antlr4 import *
from io import StringIO
from typing.io import TextIO
import sys


def serializedATN():
    with StringIO() as buf:
        buf.write("\3\u608b\ua72a\u8133\ub9ed\u417c\u3be7\u7786\u5964\2\r")
        buf.write("{\b\1\4\2\t\2\4\3\t\3\4\4\t\4\4\5\t\5\4\6\t\6\4\7\t\7")
        buf.write("\4\b\t\b\4\t\t\t\4\n\t\n\4\13\t\13\4\f\t\f\4\r\t\r\4\16")
        buf.write("\t\16\3\2\3\2\3\3\3\3\3\3\3\3\3\3\3\4\3\4\3\4\3\4\3\4")
        buf.write("\3\4\3\5\3\5\3\5\3\5\3\5\3\5\3\5\3\5\3\5\3\5\3\5\3\6\3")
        buf.write("\6\3\6\3\6\3\6\3\6\3\6\3\6\3\7\3\7\7\7@\n\7\f\7\16\7C")
        buf.write("\13\7\3\7\3\7\3\b\6\bH\n\b\r\b\16\bI\3\t\3\t\7\tN\n\t")
        buf.write("\f\t\16\tQ\13\t\3\n\3\n\3\n\5\nV\n\n\3\n\3\n\5\nZ\n\n")
        buf.write("\3\n\5\n]\n\n\5\n_\n\n\3\n\3\n\3\13\3\13\5\13e\n\13\3")
        buf.write("\13\3\13\3\f\3\f\3\r\6\rl\n\r\r\r\16\rm\3\16\3\16\3\16")
        buf.write("\3\16\7\16t\n\16\f\16\16\16w\13\16\3\16\3\16\3\16\3u\2")
        buf.write("\17\3\3\5\4\7\5\t\6\13\7\r\b\17\t\21\n\23\13\25\f\27\r")
        buf.write("\31\2\33\2\3\2\7\6\2\f\f\16\17$$^^\3\2\62;\5\2C\\aac|")
        buf.write("\6\2\62;C\\aac|\4\2\13\13\"\"\2\u0082\2\3\3\2\2\2\2\5")
        buf.write("\3\2\2\2\2\7\3\2\2\2\2\t\3\2\2\2\2\13\3\2\2\2\2\r\3\2")
        buf.write("\2\2\2\17\3\2\2\2\2\21\3\2\2\2\2\23\3\2\2\2\2\25\3\2\2")
        buf.write("\2\2\27\3\2\2\2\3\35\3\2\2\2\5\37\3\2\2\2\7$\3\2\2\2\t")
        buf.write("*\3\2\2\2\13\65\3\2\2\2\r=\3\2\2\2\17G\3\2\2\2\21K\3\2")
        buf.write("\2\2\23^\3\2\2\2\25d\3\2\2\2\27h\3\2\2\2\31k\3\2\2\2\33")
        buf.write("o\3\2\2\2\35\36\7\'\2\2\36\4\3\2\2\2\37 \7T\2\2 !\7w\2")
        buf.write("\2!\"\7n\2\2\"#\7g\2\2#\6\3\2\2\2$%\7G\2\2%&\7x\2\2&\'")
        buf.write("\7g\2\2\'(\7p\2\2()\7v\2\2)\b\3\2\2\2*+\7E\2\2+,\7q\2")
        buf.write("\2,-\7p\2\2-.\7f\2\2./\7k\2\2/\60\7v\2\2\60\61\7k\2\2")
        buf.write("\61\62\7q\2\2\62\63\7p\2\2\63\64\7u\2\2\64\n\3\2\2\2\65")
        buf.write("\66\7C\2\2\66\67\7e\2\2\678\7v\2\289\7k\2\29:\7q\2\2:")
        buf.write(";\7p\2\2;<\7u\2\2<\f\3\2\2\2=A\7$\2\2>@\n\2\2\2?>\3\2")
        buf.write("\2\2@C\3\2\2\2A?\3\2\2\2AB\3\2\2\2BD\3\2\2\2CA\3\2\2\2")
        buf.write("DE\7$\2\2E\16\3\2\2\2FH\t\3\2\2GF\3\2\2\2HI\3\2\2\2IG")
        buf.write("\3\2\2\2IJ\3\2\2\2J\20\3\2\2\2KO\t\4\2\2LN\t\5\2\2ML\3")
        buf.write("\2\2\2NQ\3\2\2\2OM\3\2\2\2OP\3\2\2\2P\22\3\2\2\2QO\3\2")
        buf.write("\2\2RS\6\n\2\2S_\5\31\r\2TV\7\17\2\2UT\3\2\2\2UV\3\2\2")
        buf.write("\2VW\3\2\2\2WZ\7\f\2\2XZ\4\16\17\2YU\3\2\2\2YX\3\2\2\2")
        buf.write("Z\\\3\2\2\2[]\5\31\r\2\\[\3\2\2\2\\]\3\2\2\2]_\3\2\2\2")
        buf.write("^R\3\2\2\2^Y\3\2\2\2_`\3\2\2\2`a\b\n\2\2a\24\3\2\2\2b")
        buf.write("e\5\31\r\2ce\5\33\16\2db\3\2\2\2dc\3\2\2\2ef\3\2\2\2f")
        buf.write("g\b\13\3\2g\26\3\2\2\2hi\13\2\2\2i\30\3\2\2\2jl\t\6\2")
        buf.write("\2kj\3\2\2\2lm\3\2\2\2mk\3\2\2\2mn\3\2\2\2n\32\3\2\2\2")
        buf.write("op\7\61\2\2pq\7,\2\2qu\3\2\2\2rt\13\2\2\2sr\3\2\2\2tw")
        buf.write("\3\2\2\2uv\3\2\2\2us\3\2\2\2vx\3\2\2\2wu\3\2\2\2xy\7,")
        buf.write("\2\2yz\7\61\2\2z\34\3\2\2\2\r\2AIOUY\\^dmu\4\3\n\2\b\2")
        buf.write("\2")
        return buf.getvalue()


class TestLexer(Lexer):

    atn = ATNDeserializer().deserialize(serializedATN())

    decisionsToDFA = [ DFA(ds, i) for i, ds in enumerate(atn.decisionToState) ]

    T__0 = 1
    T__1 = 2
    T__2 = 3
    T__3 = 4
    T__4 = 5
    STRING = 6
    INTEGER = 7
    NAME = 8
    NEWLINE = 9
    SKIP_ = 10
    UNKNOWN_CHAR = 11

    channelNames = [ u"DEFAULT_TOKEN_CHANNEL", u"HIDDEN" ]

    modeNames = [ "DEFAULT_MODE" ]

    literalNames = [ "<INVALID>",
            "'%'", "'Rule'", "'Event'", "'Conditions'", "'Actions'" ]

    symbolicNames = [ "<INVALID>",
            "STRING", "INTEGER", "NAME", "NEWLINE", "SKIP_", "UNKNOWN_CHAR" ]

    ruleNames = [ "T__0", "T__1", "T__2", "T__3", "T__4", "STRING", "INTEGER", 
                  "NAME", "NEWLINE", "SKIP_", "UNKNOWN_CHAR", "SPACES", 
                  "COMMENT" ]

    grammarFileName = "Test.g4"

    def __init__(self, input=None, output:TextIO = sys.stdout):
        super().__init__(input, output)
        self.checkVersion("4.7.1")
        self._interp = LexerATNSimulator(self, self.atn, self.decisionsToDFA, PredictionContextCache())
        self._actions = None
        self._predicates = None


        # A queue where extra tokens are pushed on (see the NEWLINE lexer rule).
        self.tokens = []

        # The stack that keeps track of the indentation level.
        self.indents = []

        # The amount of opened braces, brackets and parenthesis.
        self.opened = 0

        # The most recently produced token.
        self.last_token = None

    def emitToken(self, t):
        super().emitToken(t)
        self.tokens.append(t)

    def nextToken(self):
        if self._input.LA(1) == Token.EOF and len(self.indents) > 0:
            while len(self.tokens) > 0 and self.tokens[-1].type == Token.EOF:
                del self.tokens[-1]

            # First emit an extra line break that serves as the end of the statement.
            self.emitToken(self.common_token(TestLexer.NEWLINE, '\n'));

            # Now emit as much DEDENT tokens as needed.
            while len(self.indents) != 0:
                self.emitToken(self.create_dedent())
                del self.indents[-1]

            # Put the EOF back on the token stream.
            self.emitToken(self.common_token(Token.EOF, '<EOF>'));

        next = super().nextToken();

        if next.channel == Token.DEFAULT_CHANNEL:
            # Keep track of the last token on the default channel.
            self.last_token = next

        if len(self.tokens) == 0:
            return next
        else:
            t = self.tokens[0]
            del self.tokens[0]
            return t

    def create_dedent(self):
        from TestParser import TestParser
        dedent = self.common_token(TestParser.DEDENT, '')
        dedent.line = self.last_token.line
        return dedent

    def common_token(self, _type, text, alias=None):
        from antlr4.Token import CommonToken
        stop = self.getCharIndex() - 1
        if len(self.text) == 0:
            start = stop
        else:
            start = stop - len(self.text) + 1
        token = CommonToken(self._tokenFactorySourcePair, _type, Lexer.DEFAULT_TOKEN_CHANNEL, start, stop)
        token._text = ''
        return token

    def getIndentationCount(self, spaces):
        count = 0
        for ch in spaces:
            if ch == '\t':
                count += 8 - (count % 8)
            else:
                count += 1
        return count

    def atStartOfInput(self):
        return self._interp.column == 0 and self._interp.line == 1


    def action(self, localctx:RuleContext, ruleIndex:int, actionIndex:int):
        if self._actions is None:
            actions = dict()
            actions[8] = self.NEWLINE_action 
            self._actions = actions
        action = self._actions.get(ruleIndex, None)
        if action is not None:
            action(localctx, actionIndex)
        else:
            raise Exception("No registered action for:" + str(ruleIndex))

    def NEWLINE_action(self, localctx:RuleContext , actionIndex:int):
        if actionIndex == 0:

                import re
                from TestParser import TestParser
                new_line = re.sub(r"[^\r\n\f]+", "", self._interp.getText(self._input)) #.replaceAll("[^\r\n\f]+", "")
                spaces = re.sub(r"[\r\n\f]+", "", self._interp.getText(self._input)) #.replaceAll("[\r\n\f]+", "")
                next = self._input.LA(1)

                if self.opened > 0 or next == '\r' or next == '\n' or next == '\f':
                    self.skip()
                else:
                    self.emitToken(self.common_token(self.NEWLINE, new_line))

                    indent = self.getIndentationCount(spaces)
                    if len(self.indents) == 0:
                        previous = 0
                    else:
                        previous = self.indents[-1]

                    if indent == previous:
                        self.skip()
                    elif indent > previous:
                        self.indents.append(indent)
                        self.emitToken(self.common_token(TestParser.INDENT, spaces))
                    else:
                        while len(self.indents) > 0 and self.indents[-1] > indent:
                            self.emitToken(self.create_dedent())
                            del self.indents[-1]

     

    def sempred(self, localctx:RuleContext, ruleIndex:int, predIndex:int):
        if self._predicates is None:
            preds = dict()
            preds[8] = self.NEWLINE_sempred
            self._predicates = preds
        pred = self._predicates.get(ruleIndex, None)
        if pred is not None:
            return pred(localctx, predIndex)
        else:
            raise Exception("No registered predicate for:" + str(ruleIndex))

    def NEWLINE_sempred(self, localctx:RuleContext, predIndex:int):
            if predIndex == 0:
                return self.atStartOfInput()
         


